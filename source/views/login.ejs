<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Distribuidora Saludable</title>

    <!-- Font -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat" rel="stylesheet">

    <!-- styles -->
    <link rel="stylesheet" href="/css/main.css">
    
    <!-- Icons fontawesome.com -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">

    <style>
        header img{
            width: 20%;
            padding: 0.2rem;
        }
        
        .login{
            box-sizing: content-box;
            margin: auto;
            margin-top: 1rem;
            border: 0.2rem solid var(--pink-1);
            /* border:0.2rem solid #ebde2dfa; */
            background-color: #fff;
            max-width: 400px;
            padding: 0.8rem;
            border-radius: 0.5rem;
        }
       

        form,.more{
            margin: auto;
            padding: 1rem;
        }
        input[type="email"],input[type="password"],input[type="submit"]{
            background-color: #fff;
            font-family: 'Montserrat',sans-serif;
            font-weight: 400;
            padding: 0.5rem;
            margin: 0.3rem auto 1.5rem;
            width: 100%;
            border-radius: 0.2rem;
            border: 0.1rem solid var(--pink-1);
            
        }
        input:hover{
            border: 0.1rem solid var(--pink-2);
        }
        input[type="submit"]:hover{
            cursor: pointer;
        }
        a.register:hover{
            border: 0.1rem solid var(--pink-2);
        }
        label{
            color: rgb(102, 100, 100);
            font-weight: 700;
        }
        button{
            margin: auto;
            margin-top: 0.6rem;
            width: 100%;
            height: 2rem;
        }
        .btn{
            width: 100%;
            
        }
        header{
            background-color:var(--background-color-header);
            padding: 1rem;
            width: 100%;
        }
        header img{
            width:20%;

        }
        a{
            cursor: pointer;
            text-decoration: none;
            color: #fff;
        }
        button{
            cursor: pointer;
        }
        input[type="submit"]{
            margin-bottom: 0.3rem;
        }
        label{
            font-size: 0.8rem;
        }
        .more a{
            text-align: center;
            display: block;
            color:#272726da;
            font-size: 0.9rem;
            width: 100%;
        }
        .register{
            margin: 0.3rem auto 1.5rem;
        }
        .recover{
            text-align: center;
            color: #272726da;
        }

        @media(max-width: 425px) {
            .login{
                width: 90%;
                margin: auto;
                padding: 0.2rem;
            }
            .imput{
                width: 95%;
            }

            form{
                padding: 0.5rem;
            }
            .more{
                padding: 0 0.5rem 0.5rem 0.5rem;
            }
            
        }
        .register{
            background-color: #f8f5ceda;
            font-weight: 700;
            color: #272726da;
            padding: 0.5rem;
            margin: 0.3rem auto 1rem;
            width: 100%;
            border-radius: 0.2rem;
            border: 0.1rem solid var(--pink-1);

        }
        input[type="checkbox"]{
            color: var(--pink-2);
        }

    </style>

</head>
<body>
    <header>
        <a href="/">
            <img src="/images/logo/logoWhite.png" >
        </a>
        <a href="/">Inicio</a>
    </header>
    
    <div class="login add-shadow">
        <h3 style="text-align: center;">¡Bienvenido!</h3>
        <% if(locals.errorMessage){ %>
            <div class="errorMessage"> <%= errorMessage %>  </div>
            <% } %> 
        <form action="/login" method="POST">
            <label for="email"></label>
            <% if(locals.email){ %>
                <input type="email" value=<%= email %>  name="email">
            <% }else{ %>
                <input type="email" placeholder="Email" name="email"> 
            <% } %> 
            <label for="password"></label>
            <input class="password" type="password" placeholder="Contraseña" name="password">
            <input type="submit" value="Entrar" style="background-color:var(--pink-1);font-weight: 700;color: #272726da;"></input>
            <label>
                <input type="checkbox" checked="checked" name="remember"> Recordarme
            </label>           
        </form>
        <div class="more">
            <a href="/users/register" class="register">Registrarme</a>
            <a href="#" class="recover" style="font-weight: 600;text-decoration:underline;">Olvidé mi contraseña</a>
        </div>
    </div>
    <div class="separador"></div>
    
</body>
</html>